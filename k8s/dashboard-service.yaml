apiVersion: v1
kind: Service
metadata:
  name: neutrino-dashboard
  labels:
    app: neutrino-dashboard
spec:
  type: ClusterIP
  selector:
    app: neutrino-dashboard
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
