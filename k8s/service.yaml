apiVersion: v1
kind: Service
metadata:
  name: neutrino
  labels:
    app: neutrino
spec:
  type: ClusterIP  # Internal only - gateway discovers pods directly via k8s API
  selector:
    app: neutrino
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
